import React from 'react';
import Card from 'elements/card';

export default () => {
  return (
    <Card
      title=""
      noFade={true}
      enableLeftHeader={false}
      enableRightHeader={false}
    ></Card>
  );
};
