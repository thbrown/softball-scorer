# Block third-party install scripts by default (supply-chain hardening).
# Workspace scripts (e.g. root postinstall) still run. Re-allow specific
# packages via dependenciesMeta.built in package.json.
enableScripts: false

# Match the classic node_modules layout used by this repo.
nodeLinker: node-modules
