# Block third-party install scripts (npm has no per-package allowlist).
# After `npm install`, run `npm run setup` for local config templates.
# Prefer `yarn install` — see dependenciesMeta in package.json for allowlisted deps.
ignore-scripts=true
